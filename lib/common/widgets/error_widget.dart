import 'package:flutter/material.dart';

class ErrorW extends StatelessWidget {
  const ErrorW({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Invalid!! Error!!'),
    );
  }
}